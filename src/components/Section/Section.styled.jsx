import styled from '@emotion/styled';

const Box = styled.div`
  margin-bottom: 24px;

  &:last-child {
    margin-bottom: 0;
  }
`;

const Title = styled.h1`
  margin-bottom: 24px;

  font-size: 32px;
  font-weight: 600;
  line-height: 36px;
`;

export { Box, Title };
